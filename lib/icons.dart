import 'package:flutter/material.dart';

IconButton blueCircleIcon() {
  return IconButton(
    onPressed: () {},
    icon: const Icon(Icons.circle, color: Colors.blue, size: 30),
    padding: const EdgeInsets.only(right: 10),
    constraints: const BoxConstraints(),
  );
}

IconButton yellowCircleIcon() {
  return IconButton(
    onPressed: () {},
    icon: const Icon(Icons.circle, color: Colors.yellow, size: 30),
    padding: const EdgeInsets.only(right: 10),
    constraints: const BoxConstraints(),
  );
}

IconButton redCircleIcon() {
  return IconButton(
    onPressed: () {},
    icon: const Icon(Icons.circle, color: Colors.red, size: 30),
    padding: const EdgeInsets.only(right: 10),
    constraints: const BoxConstraints(),
  );
}
